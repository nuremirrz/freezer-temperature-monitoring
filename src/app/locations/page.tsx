export default function LocationsPage() {
  // The whole shell (sidebar, list, map, panels) is rendered by the shared
  // locations layout so the map survives navigation between screens.
  return null;
}
